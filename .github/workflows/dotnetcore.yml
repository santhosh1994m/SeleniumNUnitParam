name: APPSERVERDEPLOYMENT
on: [push]

jobs:
  build-and-deploy:
    runs-on: windows-latest
    steps: 
    - name: log in to azure
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}    
    - run: |
        az webapp list --query "[?state=='Running']"
    - name : checout the soruce code 
    - uses: actions/master@v1
